---
extensions:
  enabled:
    - Codeception\Extension\RunProcess:
        0: php -d variables_order=EGPCS -S localhost:8080 -t web
        sleep: 3
modules:
  config:
    PhpBrowser:
      url: http://localhost:8080
